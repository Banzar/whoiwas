
<div class="left_column">
	<%= image_tag @user.avatar.url, size: "180x200" %>
</div>
<div class="content_body">
	<p><%= @user.name %></p>
	<p><%= @user.email %></p>
	<p><%= @user.created_at %></p>
	<p><%= Time.now %></p>

	<% if current_user %>
		<%= current_user.name %> is the current_user... muahaha	
	<% end %>

	<%= link_to 'Back', :back %>
</div>
<div class="clear"></div>

